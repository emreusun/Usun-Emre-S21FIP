(() => {
  // select the round circles and click on them
  // and then move the images to the left
  const beerButtons = document.querySelectorAll("#navList div");

  function moveImages() {
    // get the offset of the div and do the fancy math like got
    debugger;
  }
  beerButtons.forEach(button => button.addEventListener("click", moveImages));
})();
